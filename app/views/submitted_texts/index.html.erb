<div class="bannerbra">
  <%if user_signed_in?%>
    <h1>Olá <span style="color:white;font-weight:800"><%="#{current_user.name}"%></span>, bem vindo!</h1>
  <%else%>
    <h1>Olá visitante!</h1>
  <%end%>
</div>
<div class="container">
  <br>
  <h1>Textos postados</h1>
  <div class="row">
    <div class="col-sm-10 offset-sm-1">
      <div id="submitted">
      <% if @submitteds != nil %>
        <div class="row">
          <% @submitteds.each do |submitted| %>
            <div class="col-3">
              <a href=<%="/submitted_texts/#{submitted.id}"%>>
                <div class="card">
                <div class="iconbox"><i class='fas fa-file-alt'></i></div>
                  <h6 class="cardtitle">Sugestões até: <span style="color:#0c326f"><%= format_date(submitted.deadline) %></span></h6>
                <div class="cardcontent">

                  <h5><%= submitted.service_title.truncate(45) %></h5>
                  <p><%= submitted.service.truncate(150).html_safe %></p>

                </div>
                <div class="cardlinks">
                    <%if user_signed_in?%>
                      <%if submitted.user_id == current_user.id %>
                        <h6><%= link_to 'Apagar o texto', submitted, method: :delete, data: { confirm: 'Tem certeza que quer apagar este texto?' }, class: "redlink" %></h6>
                      <%end%>
                    <%end%>
                </div>
              </div>
            </a>
          </div>
          <%end%>
        <%end%>
      </div>
    </div>
    </div>
  </div>
</div>
<div class="linkshome">
  <%= link_to 'Quero submeter um texto', new_submitted_text_path, class: "links" %>
</div>
